#!/bin/sh
#
# Save build products to the Subversion area for packaging.
# Make sure that the release area has been cleaned and
# VERS, DATE files updated
#
#set -x
SVN=~/Development/ils/svn/EMChemicals/Release/src
LIB=${GIT_REPO}/external/lib
# Prepare directories
TOOLDIR=$SVN/tools/blt-migration
mkdir -p $TOOLDIR
cd ${GIT_REPO}/blt/migration
cp migrate $TOOLDIR/migrate.sh
cp $LIB/blt-migration.jar $TOOLDIR
